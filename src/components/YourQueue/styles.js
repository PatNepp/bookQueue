import { makeStyles } from 'tss-react/mui';

export default makeStyles()(() => ({
    bookQueueContainer: {
        display: 'flex',
        flexWrap: 'wrap',
        justifyContent: 'space-around',
    }
}))